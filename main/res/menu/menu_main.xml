<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/menu_principal"
        android:icon="@android:drawable/menu_frame"
        android:title="MENÚ"
        app:showAsAction="never">
        <menu>
            <item
                android:id="@+id/home"
                android:icon="@drawable/home_icon"
                android:title="Inicio" />
            <item
                android:id="@+id/settings"
                android:icon="@drawable/settings_icon"
                android:title="Ajustes" />
            <item
                android:id="@+id/add_wk_menu"
                android:icon="@drawable/add_business"
                android:title="Añadir obras"
                android:visible="false"/>
            <item
                android:id="@+id/add_users_menu"
                android:icon="@drawable/add_users"
                android:title="Añadir usuarios"
                android:visible="false"/>
            <item
                android:id="@+id/edit_users_menu"
                android:icon="@drawable/edit_icon"
                android:title="Editar usuarios"
                android:visible="false"/>
            <item
                android:id="@+id/chat"
                android:icon="@drawable/chat_icon"
                android:title="Chat" />
            <item
                android:id="@+id/logout"
                android:icon="@android:drawable/ic_lock_power_off"
                android:title="@string/logout" />
        </menu>
    </item>

</menu>